package com.example.schoolstressreliever.justin;

import android.widget.EditText;

public class AlgorithmActivity

{

    int pointSystem;

    EditText hours;

//    Spinner textHours --> if it's the same as hours --> pointSystem = pointSystem + 1;
    // Spinner textInterests --> if it's the same as interest --> pointSystem = pointSystem + 1;
    // Toast messages
    // Put it at the top of list --> must find out how to sort recyclerviewer
}
